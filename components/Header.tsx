import { LogOut, Moon, Menu, Ghost, GhostIcon, Coffee } from 'lucide-react'
import React from 'react'
import { Button } from './ui/button'
import Link from 'next/link'

function Header() {
  return (
    <div className='flex flex-col justify-between bg-black w-full h-16'>
    </div>
  )
}

export default Header